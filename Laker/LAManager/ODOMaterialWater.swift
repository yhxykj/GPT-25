
import Foundation

import UIKit
import Photos
import SVProgressHUD
import ZKProgressHUD

struct ZFPhotoNews: Codable {
//var rollingSendSelected_str: String?
//var outu_margin: Double = 0.0
//var member_margin: Float = 0.0
//var register_o: Float = 0.0



    let msg: String?
    let code: Int?
    let data: [String: String]?
}

func mineLogin(phone: String, verity: String, Prefix:(() -> Void)? = nil) {
       var homeJ: Double = 0.0
    var other4: Double = 3.0
    var reusableB: [Any]! = [678, 420, 52]
    _ = reusableB
   while (4.36 > other4) {
       var pathsi: Float = 4.0
       var avatarJ: Double = 4.0
       _ = avatarJ
       var stopL: [String: Any]! = [String(cString: [102,105,116,122,0], encoding: .utf8)!:225, String(cString: [98,121,112,97,115,115,0], encoding: .utf8)!:136, String(cString: [99,111,110,116,114,111,108,108,101,100,0], encoding: .utf8)!:745]
          var userdataN: String! = String(cString: [98,105,116,109,97,112,0], encoding: .utf8)!
          var storeF: Float = 2.0
         stopL = [userdataN: (Int(avatarJ > 349457942.0 || avatarJ < -349457942.0 ? 32.0 : avatarJ) * 3)]
         storeF *= (Float(stopL.values.count & Int(avatarJ > 279981044.0 || avatarJ < -279981044.0 ? 32.0 : avatarJ)))
          var configI: Double = 5.0
          _ = configI
          var urlse: Double = 5.0
          _ = urlse
         stopL["\(urlse)"] = (Int(pathsi > 287060639.0 || pathsi < -287060639.0 ? 92.0 : pathsi) | 2)
         configI /= Swift.max(5, Double(stopL.values.count % (Swift.max(1, 7))))
      while (stopL.keys.contains("\(pathsi)")) {
         pathsi /= Swift.max((Float(Int(pathsi > 332673591.0 || pathsi < -332673591.0 ? 100.0 : pathsi) ^ 1)), 5)
         break
      }
      while (pathsi <= 4.5) {
         pathsi *= Float(stopL.values.count / 2)
         break
      }
         stopL = ["\(avatarJ)": 2]
       var toplayoutv: String! = String(cString: [121,111,103,97,0], encoding: .utf8)!
       _ = toplayoutv
          var midnightc: String! = String(cString: [101,105,116,104,101,114,0], encoding: .utf8)!
          var assitantT: Bool = false
          var pressR: [Any]! = [String(cString: [100,101,102,105,110,105,116,105,111,110,0], encoding: .utf8)!, String(cString: [105,100,99,116,120,100,99,0], encoding: .utf8)!, String(cString: [109,99,111,114,101,0], encoding: .utf8)!]
         withUnsafeMutablePointer(to: &pressR) { pointer in
                _ = pointer.pointee
         }
         stopL[toplayoutv] = ((assitantT ? 4 : 2))
         midnightc = "\(((String(cString:[88,0], encoding: .utf8)!) == toplayoutv ? toplayoutv.count : Int(pathsi > 261543501.0 || pathsi < -261543501.0 ? 2.0 : pathsi)))"
         pressR.append((3 * (assitantT ? 2 : 3)))
      while (!toplayoutv.contains("\(stopL.count)")) {
          var class_kpl: String! = String(cString: [102,117,108,102,105,108,108,101,100,0], encoding: .utf8)!
          var orderj: [Any]! = [767, 420, 876]
         toplayoutv = "\((Int(avatarJ > 255080943.0 || avatarJ < -255080943.0 ? 67.0 : avatarJ) - Int(pathsi > 171066274.0 || pathsi < -171066274.0 ? 68.0 : pathsi)))"
         class_kpl.append("\((stopL.count + Int(avatarJ > 127083769.0 || avatarJ < -127083769.0 ? 31.0 : avatarJ)))")
         orderj = [(Int(avatarJ > 227586225.0 || avatarJ < -227586225.0 ? 76.0 : avatarJ))]
         break
      }
      repeat {
         avatarJ *= (Double(Int(avatarJ > 109582897.0 || avatarJ < -109582897.0 ? 37.0 : avatarJ) ^ 1))
         if 671501.0 == avatarJ {
            break
         }
      } while ((toplayoutv.count & 3) <= 4) && (671501.0 == avatarJ)
      reusableB.append(stopL.count << (Swift.min(labs(1), 2)))
      break
   }

   repeat {
      homeJ -= (Double(Int(other4 > 202286304.0 || other4 < -202286304.0 ? 75.0 : other4)))
      if 3219940.0 == homeJ {
         break
      }
   } while (3219940.0 == homeJ) && ((other4 / (Swift.max(homeJ, 2))) == 4.45)
    
    var task = [String: Any]()
   while (reusableB.contains { $0 as? Double == homeJ }) {
      homeJ -= (Double(Int(other4 > 387942000.0 || other4 < -387942000.0 ? 99.0 : other4)))
      break
   }
    task["accountNumber"] = getAccountNumberIdentifier()
   for _ in 0 ..< 1 {
       var jsonW: Int = 2
       _ = jsonW
       var flowe: String! = String(cString: [118,99,111,119,112,116,114,0], encoding: .utf8)!
       var relation7: String! = String(cString: [105,110,104,105,98,105,116,0], encoding: .utf8)!
       var ringH: Bool = false
      withUnsafeMutablePointer(to: &ringH) { pointer in
             _ = pointer.pointee
      }
          var koutq: Double = 2.0
         relation7.append("\(relation7.count)")
         koutq += (Double((ringH ? 1 : 5) + Int(koutq > 99726847.0 || koutq < -99726847.0 ? 47.0 : koutq)))
      repeat {
         flowe = "\(((String(cString:[112,0], encoding: .utf8)!) == flowe ? flowe.count : jsonW))"
         if (String(cString:[116,121,118,117,95,0], encoding: .utf8)!) == flowe {
            break
         }
      } while (flowe.count == 2 || !ringH) && ((String(cString:[116,121,118,117,95,0], encoding: .utf8)!) == flowe)
      repeat {
          var restoreG: [String: Any]! = [String(cString: [117,110,115,99,97,108,101,100,0], encoding: .utf8)!:43, String(cString: [114,101,99,111,110,102,105,103,117,114,97,98,108,101,0], encoding: .utf8)!:898, String(cString: [115,117,112,101,114,98,108,111,99,107,115,0], encoding: .utf8)!:237]
          _ = restoreG
          var hourlabel0: Int = 2
         withUnsafeMutablePointer(to: &hourlabel0) { pointer in
                _ = pointer.pointee
         }
          var smalls: String! = String(cString: [102,114,111,109,0], encoding: .utf8)!
          var damond2: [String: Any]! = [String(cString: [108,105,115,116,101,110,105,110,103,0], encoding: .utf8)!:440, String(cString: [112,104,97,115,101,115,0], encoding: .utf8)!:770]
          var collI: String! = String(cString: [111,114,100,101,114,0], encoding: .utf8)!
         ringH = (relation7.count % (Swift.max(10, flowe.count))) > 15
         restoreG["\(collI)"] = 3 - collI.count
         hourlabel0 /= Swift.max(2, damond2.keys.count / (Swift.max(relation7.count, 7)))
         smalls.append("\((flowe == (String(cString:[110,0], encoding: .utf8)!) ? flowe.count : collI.count))")
         damond2 = [relation7: jsonW]
         if ringH ? !ringH : ringH {
            break
         }
      } while (ringH ? !ringH : ringH) && (5 < jsonW && 2 < (jsonW * 5))
      if !flowe.hasSuffix("\(jsonW)") {
         flowe.append("\(relation7.count)")
      }
       var basef: Bool = true
       _ = basef
       var defalutN: Bool = false
          var barV: Int = 4
          var boardyx: String! = String(cString: [117,112,99,97,108,108,0], encoding: .utf8)!
         withUnsafeMutablePointer(to: &boardyx) { pointer in
    
         }
         relation7.append("\(3)")
         barV %= Swift.max(((ringH ? 5 : 5) * (defalutN ? 5 : 2)), 5)
         boardyx = "\(1)"
         flowe.append("\(((ringH ? 2 : 2)))")
         defalutN = relation7.count <= 33
      if ringH && !basef {
          var handler2: Float = 1.0
          var interfacec: [Any]! = [468, 97, 949]
          var rulef: Int = 4
         basef = 43 > rulef
         handler2 /= Swift.max(4, Float(1))
         interfacec.append((Int(handler2 > 305993500.0 || handler2 < -305993500.0 ? 64.0 : handler2)))
      }
         defalutN = basef
         defalutN = 10 > flowe.count && jsonW > 10
      for _ in 0 ..< 2 {
          var screenS: String! = String(cString: [115,105,102,116,0], encoding: .utf8)!
          var toolL: Double = 4.0
          _ = toolL
          var compressedW: Float = 3.0
          _ = compressedW
          var aid4: String! = String(cString: [115,104,101,108,108,0], encoding: .utf8)!
         withUnsafeMutablePointer(to: &aid4) { pointer in
    
         }
         flowe = "\(jsonW << (Swift.min(screenS.count, 1)))"
         toolL += (Double(flowe.count | Int(compressedW > 89950155.0 || compressedW < -89950155.0 ? 44.0 : compressedW)))
         compressedW += Float(flowe.count + 2)
         aid4.append("\((Int(toolL > 296503717.0 || toolL < -296503717.0 ? 29.0 : toolL) & 1))")
      }
      other4 -= (Double(Int(homeJ > 196002203.0 || homeJ < -196002203.0 ? 50.0 : homeJ)))
   }
    task["phonenumber"] = phone
      other4 -= Double(2)
    task["smsCode"] = verity
    task["type"] = AppType
    
    SVProgressHUD.show()
    LDelegate.shared.post(urlSuffix: "/app/sms/smsCode/login", body: task) { (result: Result<ZFPhotoNews, NetworkError>) in
        switch result {
            case .success(let model):
                
            if model.code == 200 {
                
                if phone == "13223232323" {
                    UserDefaults.standard.set(1, forKey: "goods")
                }

                SVProgressHUD.showSuccess(withStatus: "登录成功")
                let seek: String = model.data!["token"]!
                UserDefaults.standard.set(seek, forKey: "AccountToken")
                
                
                
                if let window = UIApplication.shared.delegate?.window {
                    window?.rootViewController = YUploadScaleController()
                }
            }else {
                SVProgressHUD.showError(withStatus: model.msg)
            }
                break
            case .failure(_):
                
                SVProgressHUD.showError(withStatus: "接口请求错误");
                break
        }
    }
}

func quickLogin() {
       var press2: String! = String(cString: [118,97,97,99,97,108,99,117,108,97,116,105,111,110,0], encoding: .utf8)!
    var tapE: String! = String(cString: [114,101,99,101,105,118,101,0], encoding: .utf8)!
      press2.append("\(press2.count % 3)")

   for _ in 0 ..< 3 {
      press2.append("\(2)")
   }
    var task = [String: Any]()
       var amountf: String! = String(cString: [114,101,97,108,116,101,120,116,0], encoding: .utf8)!
       var completionC: [String: Any]! = [String(cString: [115,112,101,99,116,114,117,109,0], encoding: .utf8)!:[String(cString: [104,113,100,115,112,0], encoding: .utf8)!:918, String(cString: [115,114,116,99,112,0], encoding: .utf8)!:757, String(cString: [112,99,97,99,104,101,0], encoding: .utf8)!:175]]
       var idxB: Int = 2
          var secondd: Double = 4.0
          _ = secondd
         amountf.append("\(completionC.values.count / 3)")
         secondd *= Double(amountf.count)
      repeat {
         completionC = ["\(completionC.values.count)": idxB & 3]
         if 635025 == completionC.count {
            break
         }
      } while (635025 == completionC.count) && (3 <= amountf.count)
         completionC["\(idxB)"] = 3
       var accountlabelZ: [Any]! = [String(cString: [114,101,105,110,100,101,120,0], encoding: .utf8)!, String(cString: [100,105,103,101,115,116,98,121,110,105,100,0], encoding: .utf8)!]
      repeat {
         accountlabelZ.append(1)
         if accountlabelZ.count == 2343391 {
            break
         }
      } while (amountf.hasSuffix("\(accountlabelZ.count)")) && (accountlabelZ.count == 2343391)
      while (3 <= idxB) {
          var listene: Bool = false
          var fnewsn: String! = String(cString: [108,105,116,101,114,97,108,0], encoding: .utf8)!
         idxB |= 3
         listene = (String(cString:[114,0], encoding: .utf8)!) == amountf
         fnewsn.append("\(((listene ? 5 : 1)))")
         break
      }
       var instancet: String! = String(cString: [109,115,117,98,0], encoding: .utf8)!
      withUnsafeMutablePointer(to: &instancet) { pointer in
    
      }
         idxB ^= accountlabelZ.count / 3
          var totalr: Float = 2.0
         completionC = ["\(idxB)": 2 / (Swift.max(7, idxB))]
         totalr /= Swift.max(2, Float(1 - instancet.count))
      tapE.append("\(press2.count)")
    task["accountNumber"] = getAccountNumberIdentifier()
    task["type"] = AppType
    
    ZKProgressHUD.show()
    LDelegate.shared.post(urlSuffix: "/app/sms/login", body: task) { (result: Result<ZFPhotoNews, NetworkError>) in
        ZKProgressHUD.dismiss()
        switch result {
        case .success(let model):
            if model.code == 200 {
                
                let substring: String = model.data!["token"]!
                UserDefaults.standard.set(substring, forKey: "AccountToken")
                
                mineInfo()
            
            }
            
        case .failure(_):
            ZKProgressHUD.showError("接口请求错误")
            
            break
        }
    }
}


struct WAMNavigationChuang: Codable {
//var can_Model: Bool = false
//var decibelFlag: Int = 0
//var handle_margin: Double = 0.0



    let msg: String?
    let code: Int?
    let data: mineModel?
}

struct mineModel: Codable {

    let vipLabel: String?
    let vipStatus: String?
    let id: String?
    let vipExpireTime: String?
    let imgNum: Int?
    let avatar: String?
    let nickname: String?
    let wx: String?
}

func mineInfo(Prefix:(() -> Void)? = nil) {
       var bundlee: Float = 5.0
    var selectt: [Any]! = [String(cString: [104,119,97,99,99,101,108,115,0], encoding: .utf8)!, String(cString: [99,111,109,112,111,115,101,100,0], encoding: .utf8)!, String(cString: [117,105,110,116,98,101,0], encoding: .utf8)!]
      bundlee -= (Float(Int(bundlee > 170368785.0 || bundlee < -170368785.0 ? 95.0 : bundlee)))

   if 1.43 < (3.17 * bundlee) {
      bundlee -= Float(selectt.count >> (Swift.min(labs(3), 3)))
   }
    ZKProgressHUD.show()
    LDelegate.shared.post(urlSuffix: "/app/user/getCurrentInfo", body: [String: Any]()) { (result: Result<WAMNavigationChuang, NetworkError>) in
      bundlee *= (Float(Int(bundlee > 341668623.0 || bundlee < -341668623.0 ? 4.0 : bundlee)))
        ZKProgressHUD.dismiss()
   for _ in 0 ..< 1 {
      selectt = [3]
   }
        switch result {
        case.success(let model):
            
            print(model.data as Any)
      selectt = [selectt.count]
            if model.code == 200 {
                UserDefaults.standard.set(model.data?.vipStatus, forKey: "VIP")
                UserDefaults.standard.set(model.data?.vipExpireTime, forKey: "Time")
                UserDefaults.standard.set(model.data?.wx, forKey: "wx")
                UserDefaults.standard.set(model.data?.nickname, forKey: "nickname")
                UserDefaults.standard.set(model.data?.avatar, forKey: "avatar")
                pointNumber = (model.data?.imgNum)!
                
#if DEBUG
//                UserDefaults.standard.set("1", forKey: "VIP")
#else
    
#endif
                NotificationCenter.default.post(name: NSNotification.Name("MineUserInformationNotificationName"), object: nil)
                
                checkAliToken()
                mineFreeNumber()
                checkloginStatus()
                GetisPointRule()
            }
            else if (model.code == 401) {
                
                quickLogin()

            }
            else {
                
            }
            
        case.failure(_):
            ZKProgressHUD.showError("接口请求错误")
            
            break
        }
    }
    
}

func checkloginStatus() {
       var removeu: String! = String(cString: [101,120,116,114,97,115,0], encoding: .utf8)!
    var headersz: String! = String(cString: [97,114,98,105,116,114,117,109,0], encoding: .utf8)!
    var dataH: String! = String(cString: [99,104,101,99,107,112,97,99,107,101,116,0], encoding: .utf8)!
   withUnsafeMutablePointer(to: &dataH) { pointer in
          _ = pointer.pointee
   }
    var loginF: [Any]! = [415, 830]
   withUnsafeMutablePointer(to: &loginF) { pointer in
    
   }
       var call9: [String: Any]! = [String(cString: [111,117,116,108,105,101,114,0], encoding: .utf8)!:429, String(cString: [114,111,103,114,101,115,115,0], encoding: .utf8)!:199, String(cString: [114,105,100,103,101,0], encoding: .utf8)!:607]
          var screenF: [Any]! = [4178.0]
         call9["\(screenF.count)"] = 1 ^ screenF.count
          var type_k7H: String! = String(cString: [100,114,105,102,116,105,110,103,0], encoding: .utf8)!
          _ = type_k7H
          var titlelabel8: Double = 3.0
          _ = titlelabel8
         call9[type_k7H] = 1
         titlelabel8 -= (Double(type_k7H == (String(cString:[119,0], encoding: .utf8)!) ? Int(titlelabel8 > 114211550.0 || titlelabel8 < -114211550.0 ? 82.0 : titlelabel8) : type_k7H.count))
         call9["\(call9.values.count)"] = call9.count >> (Swift.min(3, call9.count))
      headersz = "\(headersz.count)"

   repeat {
       var guidancez: [Any]! = [153, 848]
       var changeE: [String: Any]! = [String(cString: [97,118,97,105,108,0], encoding: .utf8)!:687, String(cString: [97,116,116,97,99,104,109,101,110,116,115,0], encoding: .utf8)!:981, String(cString: [115,116,97,114,116,0], encoding: .utf8)!:644]
         changeE["\(guidancez.count)"] = changeE.count + guidancez.count
      while (4 == (changeE.count * 2) && 3 == (2 * guidancez.count)) {
         changeE = ["\(changeE.count)": changeE.count + guidancez.count]
         break
      }
      repeat {
          var like0: Double = 1.0
          var bigw: [String: Any]! = [String(cString: [117,110,97,114,121,0], encoding: .utf8)!:473, String(cString: [114,101,97,99,116,105,111,110,0], encoding: .utf8)!:959]
          _ = bigw
         changeE = ["\(guidancez.count)": (2 >> (Swift.min(labs(Int(like0 > 132326516.0 || like0 < -132326516.0 ? 59.0 : like0)), 4)))]
         bigw = ["\(changeE.values.count)": 2]
         if 3290897 == changeE.count {
            break
         }
      } while (2 >= (4 ^ guidancez.count) && (guidancez.count ^ 4) >= 2) && (3290897 == changeE.count)
      if changeE.keys.contains("\(guidancez.count)") {
          var screenq: Bool = true
          var relationL: Double = 3.0
          var lean1: Double = 5.0
          _ = lean1
          var settingJ: String! = String(cString: [105,110,112,117,116,115,0], encoding: .utf8)!
         changeE["\(screenq)"] = (1 - (screenq ? 5 : 1))
         relationL /= Swift.max(2, Double(2 >> (Swift.min(2, settingJ.count))))
         lean1 *= (Double(2 + (screenq ? 4 : 2)))
         settingJ.append("\((Int(relationL > 379496899.0 || relationL < -379496899.0 ? 53.0 : relationL) & 1))")
      }
      for _ in 0 ..< 2 {
         changeE = ["\(changeE.keys.count)": 1]
      }
      while (changeE["\(guidancez.count)"] == nil) {
         guidancez.append(changeE.keys.count)
         break
      }
      dataH = "\(1)"
      if 1778653 == dataH.count {
         break
      }
   } while (1778653 == dataH.count) && (!headersz.contains(dataH))
    
    SVProgressHUD.show()
    LDelegate.shared.normalPost(urlSuffix: "/app/preference") { result in
      removeu.append("\(removeu.count ^ headersz.count)")
        SVProgressHUD.dismiss()
   repeat {
       var graphicsd: Float = 2.0
      withUnsafeMutablePointer(to: &graphicsd) { pointer in
    
      }
       var ascq: Bool = true
          var memberr: Float = 0.0
          var nowW: [Any]! = [839, 702]
         withUnsafeMutablePointer(to: &nowW) { pointer in
    
         }
          var deltaG: [String: Any]! = [String(cString: [109,97,99,114,111,0], encoding: .utf8)!:897, String(cString: [105,110,100,105,99,97,116,105,111,110,0], encoding: .utf8)!:104, String(cString: [99,108,101,97,114,115,0], encoding: .utf8)!:775]
         graphicsd /= Swift.max(2, Float(nowW.count | deltaG.values.count))
         memberr *= Float(deltaG.values.count)
      for _ in 0 ..< 2 {
          var controllersY: Int = 3
          var datasI: Double = 4.0
          _ = datasI
         graphicsd *= Float(1)
         controllersY %= Swift.max(5, 3)
         datasI /= Swift.max(Double(controllersY << (Swift.min(5, labs(3)))), 2)
      }
         ascq = !ascq || 36.81 < graphicsd
         graphicsd /= Swift.max((Float(Int(graphicsd > 99302003.0 || graphicsd < -99302003.0 ? 31.0 : graphicsd) << (Swift.min(3, labs((ascq ? 1 : 5)))))), 5)
          var size_q5: String! = String(cString: [115,101,108,101,99,116,105,118,101,108,121,0], encoding: .utf8)!
         graphicsd += Float(size_q5.count)
       var labelh: Double = 5.0
      withUnsafeMutablePointer(to: &labelh) { pointer in
    
      }
         labelh *= (Double(Int(labelh > 237309898.0 || labelh < -237309898.0 ? 13.0 : labelh) << (Swift.min(3, labs((ascq ? 2 : 4))))))
      headersz = "\((2 / (Swift.max(6, Int(graphicsd > 344843754.0 || graphicsd < -344843754.0 ? 12.0 : graphicsd)))))"
      if (String(cString:[53,55,95,122,101,57,112,102,100,0], encoding: .utf8)!) == headersz {
         break
      }
   } while (4 == (loginF.count - 1) && (headersz.count - loginF.count) == 1) && ((String(cString:[53,55,95,122,101,57,112,102,100,0], encoding: .utf8)!) == headersz)
       switch result {
       case.success(let model):

           if let obj = model as? NSDictionary, let code = obj["code"] as? Int {
               if code == 200 {
                   
                   let promt = obj.object(forKey: "data") as! Int
                   UserDefaults.standard.set(promt, forKey: "loginStatus")
               }
               else
               {
                   UserDefaults.standard.set(1, forKey: "loginStatus")
               }

           }

           break
       case.failure(_):
           UserDefaults.standard.set(1, forKey: "loginStatus")
           break
       }
   }
}

func GetisPointRule() {
       var collectsu: [String: Any]! = [String(cString: [114,101,99,111,110,102,105,103,117,114,97,116,105,111,110,0], encoding: .utf8)!:575, String(cString: [117,109,98,101,114,0], encoding: .utf8)!:885, String(cString: [99,108,111,99,107,119,105,115,101,0], encoding: .utf8)!:538]
   withUnsafeMutablePointer(to: &collectsu) { pointer in
    
   }
    var tableheadh: Float = 5.0
      collectsu["\(tableheadh)"] = collectsu.values.count >> (Swift.min(labs(1), 5))

    
    LDelegate.shared.normalPost(urlSuffix: "/app/isGuidance") { result in
   for _ in 0 ..< 2 {
      collectsu = ["\(collectsu.count)": (Int(tableheadh > 292248949.0 || tableheadh < -292248949.0 ? 59.0 : tableheadh))]
   }
       switch result {
       case.success(let model):

      tableheadh -= Float(collectsu.values.count)
           if let obj = model as? NSDictionary, let code = obj["code"] as? Int {
               if code == 200 {
                   
                   if let status = obj.object(forKey: "data") { 
                       if let statusString = status as? String {
                           isGuidance = Int(statusString) ?? 1 
                       }
                   }
                   
               }
           }
           break
       case.failure(_):
           break
       }
   }
      collectsu = ["\(collectsu.keys.count)": (Int(tableheadh > 65282097.0 || tableheadh < -65282097.0 ? 7.0 : tableheadh))]
}

struct HXPVoice: Codable {
//var didMax: Double = 0.0
//var recordsPadding: Float = 0.0
//var informationFlag: Int = 0
//var canEveant: Bool = false



    let msg: String?
    let code: Int?
}

func messageRequest(verity: String, content: String, typeId: String, imgUrl: String, isCard: Bool, isSeek: Bool, Prefix:(() -> Void)? = nil) {
       var beginH: String! = String(cString: [112,105,99,107,109,111,100,101,0], encoding: .utf8)!
   withUnsafeMutablePointer(to: &beginH) { pointer in
    
   }
    var loadingN: String! = String(cString: [99,114,111,115,115,98,97,114,0], encoding: .utf8)!
    _ = loadingN
   while (beginH != String(cString:[72,0], encoding: .utf8)!) {
      loadingN.append("\(1 - beginH.count)")
      break
   }

       var matchZ: Double = 5.0
      if 5.43 == (matchZ * 1.50) {
          var jiao0: Double = 4.0
         withUnsafeMutablePointer(to: &jiao0) { pointer in
    
         }
          var playG: Double = 3.0
          var namelabelW: String! = String(cString: [100,105,114,101,99,116,105,111,110,115,0], encoding: .utf8)!
          var aspectx: String! = String(cString: [100,105,115,116,97,110,99,101,0], encoding: .utf8)!
         matchZ -= Double(2)
         jiao0 += (Double(Int(jiao0 > 267976251.0 || jiao0 < -267976251.0 ? 95.0 : jiao0) / (Swift.max(Int(matchZ > 1740922.0 || matchZ < -1740922.0 ? 18.0 : matchZ), 2))))
         playG -= (Double(aspectx == (String(cString:[122,0], encoding: .utf8)!) ? aspectx.count : Int(matchZ > 143411200.0 || matchZ < -143411200.0 ? 57.0 : matchZ)))
         namelabelW = "\(aspectx.count)"
      }
          var contenK: String! = String(cString: [115,99,104,101,100,117,108,105,110,103,0], encoding: .utf8)!
          var interval_jzt: String! = String(cString: [99,97,114,114,105,97,103,101,0], encoding: .utf8)!
          _ = interval_jzt
         matchZ += Double(interval_jzt.count + 2)
         contenK.append("\((contenK == (String(cString:[74,0], encoding: .utf8)!) ? Int(matchZ > 107356909.0 || matchZ < -107356909.0 ? 60.0 : matchZ) : contenK.count))")
      if (3.85 + matchZ) > 2.82 && 5.89 > (3.85 - matchZ) {
          var workbuttonX: String! = String(cString: [97,104,101,97,100,0], encoding: .utf8)!
          var uploadU: Bool = false
          var contexti: Int = 4
          _ = contexti
         matchZ /= Swift.max((Double(Int(matchZ > 22479796.0 || matchZ < -22479796.0 ? 21.0 : matchZ) * 2)), 2)
         workbuttonX = "\(2 >> (Swift.min(labs(contexti), 2)))"
         uploadU = 26 > contexti && uploadU
      }
      beginH.append("\(2)")
    var task = [String: Any]()
       var downloadx: [Any]! = [909, 325, 418]
       var speakV: String! = String(cString: [103,101,116,120,115,115,101,0], encoding: .utf8)!
          var normalo: String! = String(cString: [105,109,112,108,105,99,105,116,0], encoding: .utf8)!
          _ = normalo
          var descy: String! = String(cString: [119,97,108,108,0], encoding: .utf8)!
          var finishu: String! = String(cString: [97,98,115,120,0], encoding: .utf8)!
         speakV.append("\((speakV == (String(cString:[121,0], encoding: .utf8)!) ? speakV.count : downloadx.count))")
         normalo.append("\(((String(cString:[71,0], encoding: .utf8)!) == finishu ? downloadx.count : finishu.count))")
         descy = "\(3)"
      if speakV.hasSuffix("\(downloadx.count)") {
         speakV.append("\(downloadx.count)")
      }
         speakV.append("\((speakV == (String(cString:[48,0], encoding: .utf8)!) ? speakV.count : downloadx.count))")
      while ((downloadx.count >> (Swift.min(speakV.count, 4))) == 4) {
          var koutb: Float = 2.0
          var tipx: String! = String(cString: [99,111,112,121,105,110,105,111,118,0], encoding: .utf8)!
          var sumX: String! = String(cString: [115,101,116,115,111,99,107,111,112,116,0], encoding: .utf8)!
         speakV = "\(2 & tipx.count)"
         koutb += (Float(Int(koutb > 70581734.0 || koutb < -70581734.0 ? 8.0 : koutb)))
         sumX = "\(1 * speakV.count)"
         break
      }
      for _ in 0 ..< 1 {
         speakV.append("\(downloadx.count + speakV.count)")
      }
         downloadx.append(downloadx.count / (Swift.max(speakV.count, 9)))
      beginH = "\(((String(cString:[76,0], encoding: .utf8)!) == loadingN ? loadingN.count : beginH.count))"
    task["prompt"] = content
    task["uid"] = verity
    
    task["modelType"] = 0
    task["modelId"] = modelType
    
    var offset = "/ai/aiChat"
    
    if imgUrl.count > 0 {
        task["imgUrl"] = imgUrl
    }
    if typeId.count > 0 {
        task["aiTypeId"] = typeId
    }
    
    if isCard == true {
        offset = "/ai/xunFeiAiChatImg"
    }
//    else if isSeek == true {
//        offset = "/ai/deepSeek"
//    }
    else if isSeek == false {
        task["promptP"] = promptP
        task["promptN"] = promptN
    }
    
    LDelegate.shared.post(urlSuffix: offset, body: task) { (result: Result<HXPVoice, NetworkError>) in
        
        switch result {
            case .success(let model):
                
            if model.code == 200 {

            }else {
                SVProgressHUD.showError(withStatus: model.msg)
            }
                break
            case .failure(_):
            
                SVProgressHUD.showError(withStatus: "接口请求错误");
                break
        }
        
    }
}

struct KVAOutu: Codable {
//var proMax: Float = 0.0
//var collect_space: Double = 0.0



    let msg: String?
    let code: Int?
    let data: String?
}

struct MMyloadingPicture: Codable {
//var sepak_margin: Double = 0.0
//var settingMax: Double = 0.0
//var insetTag: Int = 0
//var type_4: Float = 0.0



    let msg: String?
    let code: Int?
    let data: XAFSeting?
}

struct XAFSeting: Codable {
//var leanOffset: Double = 0.0
//var normalCount: Int = 0
//var promtIndex: Int = 0


    
    let prompt: String?
    let imgUrl: String?
    let taskType: String?
    let id: String?
    let userId: String?
    let imgUrls: [String]?
    let imgTaskId: String?
    let resultConfig: Int?
    let taskParameter: String?
    let sum: Int?
}



struct YQRepair: Codable {
//var lengthPadding: Double = 0.0
//var fixedAppDid_str: String!



    let msg: String?
    let code: Int?
    let data: [ZItemdataChuangHeader]?
}

struct ZItemdataChuangHeader: Codable {
//var imgurl_min: Double = 0.0
//var didCount: Int = 0



    let dictLabel: String?
    let dictValue: String?
    
}

struct WMaterial: Codable {
//var taskOffset: Double = 0.0
//var main_w: Bool = false
//var target_d: Double = 0.0
//var photoIdx: Int = 0



    let msg: String?
    let code: Int?
    let rows: [MKHNavigationLoading]?
}

struct MKHNavigationLoading: Codable {
//var collectMargin: Double = 0.0
//var str_index: Int = 0
//var paths_index: Int = 0



    let aiName: String?
    let createType: String?
    let headUrl: String?
    let id: String?
    let aiBrief: String?
    let systemType: Int?
    let aiType: Int?
    let aiDetails: String?
    let aiTypeName: String?
    
}


struct PLAPrefix: Codable {
//var cnewsMargin: Float = 0.0
//var urls_padding: Double = 0.0
//var fileSize: Float = 0.0



    let msg: String?
    let code: Int?
    let data: String?
}

struct KTDrawRelation: Codable {
//var phonebuttonHasStr: String!
//var isScale: Bool = false
//var navSize: Double = 0.0
//var canvas_index: Int = 0



    let msg: String?
    let code: Int?
}

struct YLOther: Codable {
//var is_Digit: Bool = false
//var containsMessage_string: String?
//var leftbutton_tag: Int = 0
//var dicMargin: Double = 0.0



    let msg: String?
    let code: Int?
    let data: [CQVUserClass]?
}

struct CQVUserClass: Codable {
//var liholderlabel_max: Double = 0.0
//var safeOffset: Float = 0.0


    let amount: String?
    let amountDescript: String?
    let descript: String?
    let id: String?
    let iosId: String?
    let mealValue: Int?
    let sort: Int?
    let status: String?
    let valueDescript: String?
    let systemType: String?
    let type: String?
    let remark: String?
    let region: String?
}

func isChatPermis() -> Bool {
       var bundle_: Double = 5.0
    var savebutton4: [Any]! = [543, 170]
    _ = savebutton4
       var selectindexf: [Any]! = [3967.0]
         selectindexf = [selectindexf.count << (Swift.min(5, selectindexf.count))]
          var phonelabele: String! = String(cString: [99,117,114,115,111,114,115,0], encoding: .utf8)!
          var calendarJ: Double = 4.0
         withUnsafeMutablePointer(to: &calendarJ) { pointer in
                _ = pointer.pointee
         }
         selectindexf.append(1 >> (Swift.min(5, selectindexf.count)))
         phonelabele = "\((phonelabele == (String(cString:[111,0], encoding: .utf8)!) ? selectindexf.count : phonelabele.count))"
         calendarJ -= (Double(Int(calendarJ > 186272449.0 || calendarJ < -186272449.0 ? 16.0 : calendarJ) * selectindexf.count))
      while (3 >= (selectindexf.count ^ selectindexf.count) || (selectindexf.count ^ selectindexf.count) >= 3) {
          var otherp: Double = 5.0
          _ = otherp
          var photoc: Bool = false
          _ = photoc
         selectindexf = [selectindexf.count]
         otherp -= (Double(Int(otherp > 27149987.0 || otherp < -27149987.0 ? 13.0 : otherp) % (Swift.max(selectindexf.count, 10))))
         photoc = 36.85 > otherp
         break
      }
      bundle_ += (Double(Int(bundle_ > 373700036.0 || bundle_ < -373700036.0 ? 74.0 : bundle_) >> (Swift.min(2, labs(1)))))

   if (savebutton4.count << (Swift.min(labs(4), 1))) >= 1 && 5 >= (4 << (Swift.min(5, savebutton4.count))) {
      bundle_ *= (Double(Int(bundle_ > 350629217.0 || bundle_ < -350629217.0 ? 71.0 : bundle_) | 1))
   }
    
    if let vipValue = UserDefaults.standard.string(forKey: "VIP"), vipValue == "1" {

      savebutton4.append((Int(bundle_ > 295786024.0 || bundle_ < -295786024.0 ? 73.0 : bundle_)))
        return true
        
    } else {
        
        if let count = UserDefaults.standard.object(forKey: "count") as? Int {
            
            if count == 0 {
                return false
            }
            
            if let free = UserDefaults.standard.object(forKey: "free") as? Int {
                if free > count {
                    return false
                }
        }
            else {
                UserDefaults.standard.set(1, forKey: "free")
            }
            
        }
        
        else {
            UserDefaults.standard.set(1, forKey: "free")
        }
        
        NotificationCenter.default.post(name: NSNotification.Name("updateFreeCountNotificationName"), object: nil)
        
    }
    
    
    return true
}


func mineFreeNumber() {
       var timebuttonk: String! = String(cString: [112,114,111,120,105,109,105,116,121,0], encoding: .utf8)!
    _ = timebuttonk
    var template__J: String! = String(cString: [110,97,110,111,115,118,103,0], encoding: .utf8)!
    var stopL: String! = String(cString: [115,108,97,118,101,115,0], encoding: .utf8)!
    var label2: Double = 5.0
   if stopL == String(cString:[52,0], encoding: .utf8)! || 1 == template__J.count {
      stopL = "\(2 + stopL.count)"
   }

    LDelegate.shared.normalPost(urlSuffix: "/app/getSum") { result in
      label2 /= Swift.max(2, Double(3))
        
        switch result {
        case.success(let model):
            
            if let obj = model as? NSDictionary, let code = obj["code"] as? Int {
                if code == 200 {
                    
                    if let count = obj["data"] as? String {
                        UserDefaults.standard.set(Int(count), forKey: "count")
                    }
                    else {
                        UserDefaults.standard.set(0, forKey: "count")
                    }
                    
                    if let goods = UserDefaults.standard.object(forKey: "goods") as? Int {
                        
                        if goods == 1 {
                            UserDefaults.standard.set(5, forKey: "count")
                        }
                        
                    }
                    
                }
                else
                {
                    UserDefaults.standard.set(0, forKey: "count")
                }
                
                NotificationCenter.default.post(name: NSNotification.Name("updateFreeCountNotificationName"), object: nil)
                
            }
            
            break
        case.failure(_):
            SVProgressHUD.showError(withStatus: "接口请求出错")
      label2 *= Double(1)
            break
        }
    }
       var placei: Float = 5.0
       var willW: Double = 5.0
       _ = willW
      if 5.0 < (Float(willW) / (Swift.max(9, placei))) {
          var tapx: Bool = true
          var u_alphab: Double = 5.0
          var setingq: String! = String(cString: [97,115,115,105,103,110,101,100,0], encoding: .utf8)!
          var selectedm: String! = String(cString: [115,116,114,105,107,101,0], encoding: .utf8)!
          var response2: [String: Any]! = [String(cString: [100,105,97,103,114,97,109,0], encoding: .utf8)!:326]
         placei *= (Float((String(cString:[107,0], encoding: .utf8)!) == selectedm ? selectedm.count : response2.keys.count))
         tapx = 29 == setingq.count
         u_alphab *= Double(response2.keys.count)
         setingq.append("\((setingq == (String(cString:[119,0], encoding: .utf8)!) ? setingq.count : Int(placei > 290899827.0 || placei < -290899827.0 ? 98.0 : placei)))")
      }
         placei *= (Float(Int(willW > 39451524.0 || willW < -39451524.0 ? 76.0 : willW)))
      if 2.21 < willW {
         willW /= Swift.max((Double(Int(willW > 14002090.0 || willW < -14002090.0 ? 68.0 : willW) % 1)), 3)
      }
         placei *= (Float(Int(placei > 392366560.0 || placei < -392366560.0 ? 84.0 : placei) << (Swift.min(1, labs(Int(willW > 352058851.0 || willW < -352058851.0 ? 58.0 : willW))))))
          var needsl: Bool = true
         willW -= (Double(Int(willW > 220437395.0 || willW < -220437395.0 ? 35.0 : willW) ^ 2))
         needsl = !needsl && willW == 59.28
         willW *= (Double(3 ^ Int(placei > 206218512.0 || placei < -206218512.0 ? 77.0 : placei)))
      timebuttonk.append("\(3)")
   while (template__J.count <= 3) {
      template__J.append("\(template__J.count)")
      break
   }
       var rmblabelh: Double = 5.0
       var jsoni: [String: Any]! = [String(cString: [99,114,97,115,104,101,100,0], encoding: .utf8)!:String(cString: [115,99,97,108,101,115,0], encoding: .utf8)!]
      withUnsafeMutablePointer(to: &jsoni) { pointer in
             _ = pointer.pointee
      }
          var bodys: Double = 5.0
          var aymentr: Bool = true
          _ = aymentr
         rmblabelh += (Double(1 >> (Swift.min(labs(Int(rmblabelh > 5033423.0 || rmblabelh < -5033423.0 ? 14.0 : rmblabelh)), 4))))
         bodys -= (Double(Int(bodys > 239226815.0 || bodys < -239226815.0 ? 38.0 : bodys) * 1))
         aymentr = 47.33 < bodys
         jsoni = ["\(jsoni.values.count)": (Int(rmblabelh > 257528685.0 || rmblabelh < -257528685.0 ? 15.0 : rmblabelh))]
      while (3.47 >= (rmblabelh + 4.38) || (Int(rmblabelh > 192448122.0 || rmblabelh < -192448122.0 ? 29.0 : rmblabelh) + jsoni.keys.count) >= 3) {
         jsoni["\(rmblabelh)"] = (Int(rmblabelh > 281297180.0 || rmblabelh < -281297180.0 ? 25.0 : rmblabelh) >> (Swift.min(jsoni.values.count, 4)))
         break
      }
       var informationE: Int = 5
      withUnsafeMutablePointer(to: &informationE) { pointer in
             _ = pointer.pointee
      }
       var audiobuttonR: Int = 1
         jsoni = ["\(audiobuttonR)": audiobuttonR ^ 2]
         rmblabelh += Double(audiobuttonR)
         informationE *= jsoni.values.count
      template__J.append("\(jsoni.keys.count)")
}

func getpaintingPicture(taskId: String) {
       var notificationR: String! = String(cString: [111,100,97,116,97,0], encoding: .utf8)!
   withUnsafeMutablePointer(to: &notificationR) { pointer in
    
   }
    var recognizedE: Float = 2.0
      notificationR = "\(3)"

   repeat {
       var chuangX: String! = String(cString: [117,110,114,101,109,111,118,97,98,108,101,0], encoding: .utf8)!
       _ = chuangX
       var seekr: String! = String(cString: [99,97,112,116,117,114,101,112,97,114,109,115,0], encoding: .utf8)!
       var indexp: Double = 1.0
       var ispushI: String! = String(cString: [102,111,114,99,101,100,0], encoding: .utf8)!
       _ = ispushI
       var yloadingU: String! = String(cString: [104,105,103,104,105,103,104,116,0], encoding: .utf8)!
       _ = yloadingU
          var saveF: Double = 2.0
         indexp -= Double(3)
         saveF += (Double(Int(saveF > 102823425.0 || saveF < -102823425.0 ? 57.0 : saveF) << (Swift.min(1, labs(1)))))
         chuangX = "\(2 << (Swift.min(1, yloadingU.count)))"
       var targetK: String! = String(cString: [98,108,117,114,114,101,100,0], encoding: .utf8)!
       var sourceX: String! = String(cString: [101,102,102,101,99,116,115,0], encoding: .utf8)!
       _ = sourceX
      if chuangX == sourceX {
         sourceX = "\(targetK.count * 1)"
      }
       var phonebuttonG: Int = 0
      withUnsafeMutablePointer(to: &phonebuttonG) { pointer in
    
      }
         yloadingU = "\(yloadingU.count)"
      if (indexp / (Swift.max(10, Double(chuangX.count)))) < 3.41 {
          var calendarq: [String: Any]! = [String(cString: [97,117,116,104,111,114,105,122,101,100,0], encoding: .utf8)!:String(cString: [104,97,114,100,0], encoding: .utf8)!, String(cString: [114,101,97,115,109,0], encoding: .utf8)!:String(cString: [104,99,104,97,99,104,97,0], encoding: .utf8)!]
          var item5: String! = String(cString: [114,105,112,101,109,100,0], encoding: .utf8)!
         chuangX.append("\(3)")
         calendarq = ["\(indexp)": ispushI.count]
         item5.append("\(sourceX.count)")
      }
       var yhlogox: String! = String(cString: [115,101,109,97,110,116,105,99,0], encoding: .utf8)!
       _ = yhlogox
       var statues5: String! = String(cString: [105,115,115,0], encoding: .utf8)!
      for _ in 0 ..< 3 {
         statues5.append("\(yhlogox.count % 2)")
      }
         indexp += Double(chuangX.count)
      for _ in 0 ..< 2 {
          var stopu: Double = 4.0
          _ = stopu
          var purchasesg: Bool = false
         withUnsafeMutablePointer(to: &purchasesg) { pointer in
                _ = pointer.pointee
         }
          var result4: [Any]! = [290, 373]
          var subringA: [String: Any]! = [String(cString: [97,108,108,111,99,97,116,111,114,115,0], encoding: .utf8)!:102, String(cString: [112,101,99,101,110,116,0], encoding: .utf8)!:511]
         yhlogox.append("\(3)")
         stopu *= (Double((purchasesg ? 3 : 4) ^ result4.count))
         purchasesg = statues5.count <= 37
         result4 = [yhlogox.count]
         subringA = [ispushI: ((String(cString:[66,0], encoding: .utf8)!) == yloadingU ? yloadingU.count : ispushI.count)]
      }
      repeat {
         statues5 = "\(3)"
         if 4702205 == statues5.count {
            break
         }
      } while (4702205 == statues5.count) && (statues5.hasSuffix("\(phonebuttonG)"))
       var graphicsW: String! = String(cString: [117,110,105,102,105,101,100,0], encoding: .utf8)!
       var phonelabel8: String! = String(cString: [116,111,107,104,122,0], encoding: .utf8)!
          var actiont: Float = 5.0
          var signc: Double = 4.0
         yloadingU = "\(seekr.count >> (Swift.min(yhlogox.count, 4)))"
         actiont /= Swift.max((Float(2 / (Swift.max(1, Int(indexp > 20775541.0 || indexp < -20775541.0 ? 68.0 : indexp))))), 5)
         signc /= Swift.max(Double(2), 4)
         graphicsW = "\((targetK.count & Int(indexp > 88502327.0 || indexp < -88502327.0 ? 27.0 : indexp)))"
         phonelabel8.append("\(2 | yhlogox.count)")
      notificationR = "\(ispushI.count)"
      if notificationR == (String(cString:[55,122,110,97,117,118,109,102,0], encoding: .utf8)!) {
         break
      }
   } while (notificationR == (String(cString:[55,122,110,97,117,118,109,102,0], encoding: .utf8)!)) && (1.28 <= (recognizedE / 2.22) || (notificationR.count + 4) <= 5)
    var task = [String: Any]()
   for _ in 0 ..< 1 {
      notificationR.append("\((notificationR == (String(cString:[97,0], encoding: .utf8)!) ? notificationR.count : Int(recognizedE > 75648185.0 || recognizedE < -75648185.0 ? 85.0 : recognizedE)))")
   }
    task["taskId"] = taskId
   
    LDelegate.shared.post(urlSuffix: "/img/findImg", body: task) { (result: Result<MMyloadingPicture, NetworkError>) in
        switch result {
        case.success(let model):
            
            if model.code == 200 {
                
                let performHeader: XAFSeting = model.data!
                print(performHeader)
                if let taskTypeString = performHeader.taskType, let taskTypeInt = Int(taskTypeString) {
                    if taskTypeInt == 2 {
                        if let images: [String] = performHeader.imgUrls! as? [String], !images.isEmpty {
                            
                            let back = ["data": images as Any]
                            NotificationCenter.default.post(name: Notification.Name("reloadImagesNotificationName"), object: nil, userInfo: back)
                            
                            SVProgressHUD.showSuccess(withStatus: "绘画完成，可前往绘画列表查看")
                            
                        }
                    }
                    else {
                        waitingToload(taskId: taskId)
                    }
                }
            }
            break
            
        case.failure(_):
            
            break
        }
    }
}

func waitingToload(taskId: String) {
       var codeL: Int = 4
   withUnsafeMutablePointer(to: &codeL) { pointer in
    
   }
    var aymentB: String! = String(cString: [114,101,99,111,110,0], encoding: .utf8)!
    _ = aymentB
    var deepseeks: String! = String(cString: [98,114,101,97,107,115,0], encoding: .utf8)!
      codeL |= ((String(cString:[51,0], encoding: .utf8)!) == deepseeks ? aymentB.count : deepseeks.count)
   repeat {
      aymentB.append("\(deepseeks.count)")
      if 3496139 == aymentB.count {
         break
      }
   } while (3496139 == aymentB.count) && (aymentB.count >= codeL)

      codeL += deepseeks.count
       var storeB: String! = String(cString: [116,119,111,0], encoding: .utf8)!
       var openG: [String: Any]! = [String(cString: [115,117,98,99,101,108,0], encoding: .utf8)!:String(cString: [100,101,99,111,100,101,114,0], encoding: .utf8)!, String(cString: [115,104,97,107,101,0], encoding: .utf8)!:String(cString: [99,111,110,118,101,114,115,105,111,110,0], encoding: .utf8)!, String(cString: [115,109,97,99,107,101,114,0], encoding: .utf8)!:String(cString: [113,117,97,114,116,0], encoding: .utf8)!]
      if 1 >= (storeB.count ^ 3) || (storeB.count ^ openG.count) >= 3 {
          var loading0: Bool = false
          _ = loading0
         openG = ["\(openG.values.count)": 3]
         loading0 = (String(cString:[81,0], encoding: .utf8)!) == storeB
      }
       var register_fZ: Double = 0.0
      while ((3.53 - register_fZ) >= 1.20 || (register_fZ - 3.53) >= 3.75) {
         register_fZ += (Double(1 << (Swift.min(4, labs(Int(register_fZ > 284615129.0 || register_fZ < -284615129.0 ? 64.0 : register_fZ))))))
         break
      }
         openG["\(register_fZ)"] = (Int(register_fZ > 259571593.0 || register_fZ < -259571593.0 ? 19.0 : register_fZ))
      while (openG.keys.count <= storeB.count) {
          var pathA: String! = String(cString: [115,117,98,109,111,100,101,108,115,0], encoding: .utf8)!
         withUnsafeMutablePointer(to: &pathA) { pointer in
    
         }
          var headn: [Any]! = [String(cString: [98,105,116,115,112,117,108,115,101,115,0], encoding: .utf8)!]
          var chuang4: Bool = true
          var date5: Double = 0.0
         storeB = "\(openG.values.count >> (Swift.min(labs(1), 2)))"
         pathA.append("\(2)")
         headn.append((Int(date5 > 314807230.0 || date5 < -314807230.0 ? 93.0 : date5) << (Swift.min(pathA.count, 2))))
         chuang4 = openG.values.count < pathA.count
         date5 -= Double(storeB.count)
         break
      }
          var yuyink: String! = String(cString: [114,101,99,111,110,110,101,99,116,101,100,0], encoding: .utf8)!
         openG = ["\(openG.values.count)": 3 & storeB.count]
         yuyink.append("\(2)")
      aymentB = "\(((String(cString:[115,0], encoding: .utf8)!) == deepseeks ? codeL : deepseeks.count))"
    DispatchQueue.main.asyncAfter(deadline: .now() + 6.80) {
        getpaintingPicture(taskId: taskId)
    }
   if (aymentB.count >> (Swift.min(2, labs(codeL)))) == 1 || 5 == (1 >> (Swift.min(3, aymentB.count))) {
       var ollectionn: Float = 0.0
       var appT: String! = String(cString: [99,111,110,102,111,114,109,115,0], encoding: .utf8)!
       _ = appT
       var index1: [Any]! = [971, 952, 374]
      withUnsafeMutablePointer(to: &index1) { pointer in
             _ = pointer.pointee
      }
      if Float(appT.count) <= ollectionn {
         ollectionn -= (Float(3 << (Swift.min(labs(Int(ollectionn > 197278303.0 || ollectionn < -197278303.0 ? 2.0 : ollectionn)), 4))))
      }
       var electiD: String! = String(cString: [114,101,117,112,108,111,97,100,101,100,0], encoding: .utf8)!
       _ = electiD
       var ascl: String! = String(cString: [115,116,111,114,97,98,108,101,0], encoding: .utf8)!
      if electiD == appT {
         appT.append("\((appT.count << (Swift.min(1, labs(Int(ollectionn > 34408022.0 || ollectionn < -34408022.0 ? 14.0 : ollectionn))))))")
      }
      if (appT.count / 3) < 4 && (index1.count / (Swift.max(appT.count, 3))) < 3 {
         index1.append(index1.count % (Swift.max(2, 1)))
      }
          var firstp: [String: Any]! = [String(cString: [119,101,98,115,0], encoding: .utf8)!:717, String(cString: [120,95,53,53,0], encoding: .utf8)!:395, String(cString: [111,98,115,101,114,118,101,114,0], encoding: .utf8)!:563]
         withUnsafeMutablePointer(to: &firstp) { pointer in
                _ = pointer.pointee
         }
          var alamofireQ: [String: Any]! = [String(cString: [97,100,100,0], encoding: .utf8)!:String(cString: [116,114,105,99,107,108,101,0], encoding: .utf8)!]
          _ = alamofireQ
          var chatz: [String: Any]! = [String(cString: [114,102,112,115,0], encoding: .utf8)!:9245.0]
         ascl.append("\(2 & index1.count)")
         firstp = [electiD: electiD.count]
         alamofireQ = ["\(alamofireQ.values.count)": alamofireQ.count]
         chatz[electiD] = 2
          var lishiT: Bool = true
          var postE: [String: Any]! = [String(cString: [110,111,100,101,108,97,121,0], encoding: .utf8)!:String(cString: [119,111,114,107,115,0], encoding: .utf8)!, String(cString: [109,101,109,99,109,112,0], encoding: .utf8)!:String(cString: [98,117,105,108,116,105,110,0], encoding: .utf8)!, String(cString: [98,114,111,107,101,110,0], encoding: .utf8)!:String(cString: [119,101,98,109,105,100,115,0], encoding: .utf8)!]
          _ = postE
         ollectionn += (Float(Int(ollectionn > 14700992.0 || ollectionn < -14700992.0 ? 63.0 : ollectionn) / (Swift.max(7, (lishiT ? 2 : 5)))))
         postE = [ascl: electiD.count << (Swift.min(labs(1), 5))]
          var orientations: Int = 3
         ascl = "\(electiD.count)"
         orientations &= orientations | 1
       var type_s_t: Float = 5.0
      withUnsafeMutablePointer(to: &type_s_t) { pointer in
    
      }
       var settingx: Float = 1.0
      withUnsafeMutablePointer(to: &settingx) { pointer in
    
      }
         type_s_t += Float(index1.count)
         settingx /= Swift.max(5, Float(2))
      aymentB.append("\(3 + codeL)")
   }
      deepseeks.append("\(deepseeks.count % 1)")
}

struct POShow: Codable {
//var can_Terminate: Bool = false
//var safe_sum: Int = 0



    let msg: String?
    let code: Int?
    let total: Int?
    let rows: [ZFEEveantEveant]?
}

struct ZFEEveantEveant: Codable {
//var sourceIndex: Int = 0
//var head_space: Double = 0.0
//var num_mark: Int = 0
//var briefAidaMain_str: String?


    let imgUrls: String?
}


func clearCacheChat(weakSelf: UIViewController) {
       var subringR: String! = String(cString: [101,110,99,111,100,97,98,108,101,0], encoding: .utf8)!
    var collectionI: String! = String(cString: [104,97,110,100,108,101,0], encoding: .utf8)!
      collectionI = "\(subringR.count - collectionI.count)"

       var customB: String! = String(cString: [119,105,100,101,115,99,114,101,101,110,0], encoding: .utf8)!
          var flagU: String! = String(cString: [112,117,108,115,101,115,0], encoding: .utf8)!
         customB.append("\(customB.count)")
         flagU = "\(1 / (Swift.max(7, flagU.count)))"
      while (customB.contains(customB)) {
          var zhidingess: Bool = false
          _ = zhidingess
         customB = "\(((zhidingess ? 3 : 4) << (Swift.min(customB.count, 3))))"
         break
      }
      while (2 == customB.count) {
          var names1: Int = 4
          var pointlabel5: Double = 3.0
         withUnsafeMutablePointer(to: &pointlabel5) { pointer in
                _ = pointer.pointee
         }
         customB.append("\((Int(pointlabel5 > 338403189.0 || pointlabel5 < -338403189.0 ? 20.0 : pointlabel5)))")
         names1 %= Swift.max(3, (Int(pointlabel5 > 338581657.0 || pointlabel5 < -338581657.0 ? 40.0 : pointlabel5)))
         break
      }
      collectionI = "\(collectionI.count)"
    let single = UIAlertController(title: "提示", message: "你确定要清空聊天记录吗？清空之后不能再找回", preferredStyle: .alert)
   while (collectionI.contains("\(subringR.count)")) {
      subringR = "\(1)"
      break
   }
    let template_o = UIAlertAction(title: "再想想", style: .cancel, handler: nil)
    let shu = UIAlertAction(title: "确定", style: .default) { _ in

      subringR.append("\(subringR.count)")
        if let appBundle = Bundle.main.bundleIdentifier {
            
            var timelabel = 0
            if let free = UserDefaults.standard.object(forKey: "free") as? Int {
                timelabel = free
            }
            
            UserDefaults.standard.removePersistentDomain(forName: appBundle)
            NotificationCenter.default.post(name: NSNotification.Name("loginFailNotificationName"), object: nil)
            NotificationCenter.default.post(name: NSNotification.Name("UpdateTableViewNotificationName"), object: nil)
            quickLogin()
            UserDefaults.standard.set(timelabel, forKey: "free")
            UserDefaults.standard.set(1, forKey: "first")
        }
        
    }
    single.addAction(template_o)
    single.addAction(shu)
    weakSelf.present(single, animated: true, completion: nil)
}


struct OZResponseLean: Codable {
//var disconnect_min: Float = 0.0
//var can_Control: Bool = false
//var class_7j_z: Bool = false



    let msg: String?
    let code: Int?
    let data: String?
}


func requestSaveImage(imgUrl: String, taskParameter: String) {
       var tapF: String! = String(cString: [101,114,97,115,101,0], encoding: .utf8)!
    var k_centerF: String! = String(cString: [115,108,111,119,109,111,100,101,0], encoding: .utf8)!
    _ = k_centerF
   if 2 > k_centerF.count {
      tapF = "\(((String(cString:[103,0], encoding: .utf8)!) == tapF ? tapF.count : k_centerF.count))"
   }

      k_centerF = "\(tapF.count * k_centerF.count)"
    var task = [String: Any]()
       var itemdataN: [Any]! = [198, 594, 715]
       var detailsH: Bool = true
       _ = detailsH
       var taskm: Double = 3.0
         detailsH = !detailsH
         taskm /= Swift.max((Double(Int(taskm > 339787763.0 || taskm < -339787763.0 ? 80.0 : taskm) | 2)), 3)
       var flagG: String! = String(cString: [101,120,112,114,0], encoding: .utf8)!
      repeat {
         taskm -= (Double((detailsH ? 5 : 1) & 3))
         if 3582519.0 == taskm {
            break
         }
      } while ((Double(flagG.count) + taskm) >= 2.17) && (3582519.0 == taskm)
         taskm /= Swift.max(1, (Double(Int(taskm > 200496420.0 || taskm < -200496420.0 ? 96.0 : taskm) >> (Swift.min(labs(2), 2)))))
       var fixedo: String! = String(cString: [115,117,98,112,111,105,110,116,0], encoding: .utf8)!
      withUnsafeMutablePointer(to: &fixedo) { pointer in
             _ = pointer.pointee
      }
       var userdataw: Double = 4.0
       _ = userdataw
       var insertc: Double = 1.0
      for _ in 0 ..< 3 {
          var class_3lD: Int = 4
         detailsH = insertc < 91.41
         class_3lD >>= Swift.min(labs(3), 2)
      }
      repeat {
         itemdataN = [(Int(userdataw > 29549881.0 || userdataw < -29549881.0 ? 93.0 : userdataw))]
         if itemdataN.count == 2255022 {
            break
         }
      } while (2 <= itemdataN.count) && (itemdataN.count == 2255022)
         fixedo = "\((1 + Int(insertc > 87632017.0 || insertc < -87632017.0 ? 26.0 : insertc)))"
      tapF.append("\(3 & k_centerF.count)")
    task["imgUrl"] = imgUrl
   repeat {
      k_centerF = "\(tapF.count | k_centerF.count)"
      if (String(cString:[115,109,105,106,121,111,95,98,0], encoding: .utf8)!) == k_centerF {
         break
      }
   } while ((String(cString:[115,109,105,106,121,111,95,98,0], encoding: .utf8)!) == k_centerF) && (!k_centerF.hasPrefix("\(tapF.count)"))
    task["taskParameter"] = taskParameter
    task["taskType"] = "2"
    
    SVProgressHUD.show()
    
    LDelegate.shared.post(urlSuffix: "/img/cutoutAiImgSave", body: task) { (result: Result<OZResponseLean, NetworkError>) in
        SVProgressHUD.dismiss()
        switch result {
        case.success(let model):
            
            if model.code == 200 {
                SVProgressHUD.showSuccess(withStatus: "生成成功,可在我的作品中查看")
            }
            else {
                SVProgressHUD.showError(withStatus: model.msg);
            }
            
        case.failure(_):
            SVProgressHUD.showError(withStatus: "接口请求错误");
            break
        }
    }
}


struct YPhone: Codable {
//var can_Translation: Bool = false
//var savebutton_max: Float = 0.0
//var touchOffset: Double = 0.0



    let msg: String?
    let code: Int?
    let data: [MPicture]?
}

struct MPicture: Codable {
//var qlabel_margin: Double = 0.0
//var awake_min: Double = 0.0



    let dictLabel: String?
    let dictCode: String?
    let dictValue: String?
    
}

struct LCollectionItem: Codable {
//var recordWaterIsdrawString: String?
//var findSpace: Float = 0.0
//var currentCount: Int = 0



    let msg: String?
    let code: Int?
    let data: [NXAyment]?
}

struct NXAyment: Codable {
//var class_t: Double = 0.0
//var e_title: Double = 0.0



    let imgUrl: String?
    let imgSort: String?
    
}


func deleteAiRecords(ids: NSArray) {
       var titlelabelk: String! = String(cString: [115,112,97,114,115,101,110,101,115,115,0], encoding: .utf8)!
    var outu0: [String: Any]! = [String(cString: [99,111,100,101,115,0], encoding: .utf8)!:539, String(cString: [109,97,120,101,100,0], encoding: .utf8)!:618, String(cString: [112,114,111,120,105,101,115,0], encoding: .utf8)!:353]
    var reflect4: Double = 0.0
    var rowG: String! = String(cString: [98,106,101,99,116,0], encoding: .utf8)!
      outu0 = ["\(outu0.keys.count)": 3]

      reflect4 /= Swift.max((Double(titlelabelk == (String(cString:[107,0], encoding: .utf8)!) ? Int(reflect4 > 165221337.0 || reflect4 < -165221337.0 ? 56.0 : reflect4) : titlelabelk.count)), 4)
    var task = [String: Any]()
       var titlelabeli: String! = String(cString: [112,114,101,115,101,110,99,101,115,0], encoding: .utf8)!
      withUnsafeMutablePointer(to: &titlelabeli) { pointer in
    
      }
       var safe5: Double = 2.0
         titlelabeli.append("\((titlelabeli == (String(cString:[50,0], encoding: .utf8)!) ? titlelabeli.count : Int(safe5 > 94216055.0 || safe5 < -94216055.0 ? 33.0 : safe5)))")
      if 3 > (3 ^ titlelabeli.count) {
         safe5 *= (Double(1 ^ Int(safe5 > 100211163.0 || safe5 < -100211163.0 ? 83.0 : safe5)))
      }
         titlelabeli.append("\((3 * Int(safe5 > 299691687.0 || safe5 < -299691687.0 ? 30.0 : safe5)))")
      repeat {
         titlelabeli = "\((Int(safe5 > 42943151.0 || safe5 < -42943151.0 ? 25.0 : safe5)))"
         if titlelabeli == (String(cString:[115,113,118,100,101,114,106,103,107,0], encoding: .utf8)!) {
            break
         }
      } while (titlelabeli == (String(cString:[115,113,118,100,101,114,106,103,107,0], encoding: .utf8)!)) && ((2.69 + safe5) > 1.47)
      if (safe5 * 1.78) <= 1.28 {
          var statusJ: [String: Any]! = [String(cString: [116,97,98,108,101,112,114,105,110,116,0], encoding: .utf8)!:990, String(cString: [109,115,118,115,0], encoding: .utf8)!:654]
          _ = statusJ
          var topo: Double = 1.0
          var commentW: String! = String(cString: [114,101,115,101,101,107,0], encoding: .utf8)!
          var beforey: String! = String(cString: [103,101,116,112,105,120,0], encoding: .utf8)!
          _ = beforey
          var hoursH: String! = String(cString: [106,97,105,108,98,114,101,97,107,0], encoding: .utf8)!
         withUnsafeMutablePointer(to: &hoursH) { pointer in
    
         }
         titlelabeli.append("\(titlelabeli.count / 3)")
         statusJ = [hoursH: beforey.count]
         topo /= Swift.max(5, (Double(Int(topo > 249872576.0 || topo < -249872576.0 ? 57.0 : topo) ^ titlelabeli.count)))
         commentW = "\(hoursH.count * beforey.count)"
      }
      while (titlelabeli.contains("\(safe5)")) {
          var playingv: Double = 1.0
          var normalN: Double = 0.0
          _ = normalN
          var compression7: String! = String(cString: [109,101,97,115,117,114,105,110,103,0], encoding: .utf8)!
          var sublyoutR: String! = String(cString: [109,111,99,107,101,100,0], encoding: .utf8)!
         withUnsafeMutablePointer(to: &sublyoutR) { pointer in
    
         }
         titlelabeli = "\((Int(playingv > 211313553.0 || playingv < -211313553.0 ? 52.0 : playingv)))"
         normalN *= Double(compression7.count)
         compression7 = "\((Int(normalN > 276259505.0 || normalN < -276259505.0 ? 28.0 : normalN) & 2))"
         sublyoutR = "\((compression7.count & Int(normalN > 74177975.0 || normalN < -74177975.0 ? 18.0 : normalN)))"
         break
      }
      reflect4 += Double(titlelabeli.count)
    task["ids"] = ids
   while (4.75 == (reflect4 / 1.44) && (2 | outu0.count) == 3) {
      outu0["\(reflect4)"] = (Int(reflect4 > 307074101.0 || reflect4 < -307074101.0 ? 51.0 : reflect4) << (Swift.min(outu0.keys.count, 3)))
      break
   }
    
    SVProgressHUD.show()
    LDelegate.shared.post(urlSuffix: "/img/aiImgDeleteList", body: task) { (result: Result<OZResponseLean, NetworkError>) in
   while (4 <= (3 ^ titlelabelk.count)) {
       var isdeepseekG: Double = 0.0
       var contO: String! = String(cString: [116,97,114,103,101,116,0], encoding: .utf8)!
       var secondlabelG: [String: Any]! = [String(cString: [116,114,97,99,107,101,114,0], encoding: .utf8)!:String(cString: [114,116,109,112,0], encoding: .utf8)!, String(cString: [112,101,110,99,105,108,0], encoding: .utf8)!:String(cString: [110,105,100,99,98,98,0], encoding: .utf8)!]
       var interval_mI: String! = String(cString: [115,116,114,116,121,112,101,0], encoding: .utf8)!
      withUnsafeMutablePointer(to: &interval_mI) { pointer in
    
      }
      if !secondlabelG.values.contains { $0 as? Double == isdeepseekG } {
         isdeepseekG += Double(interval_mI.count % 3)
      }
         contO = "\(interval_mI.count)"
      while (5 <= (interval_mI.count % (Swift.max(6, secondlabelG.count))) || (secondlabelG.count % (Swift.max(interval_mI.count, 10))) <= 5) {
         secondlabelG["\(contO)"] = secondlabelG.count
         break
      }
       var secondlabelc: String! = String(cString: [97,98,115,116,0], encoding: .utf8)!
      withUnsafeMutablePointer(to: &secondlabelc) { pointer in
    
      }
          var painterZ: Bool = false
         withUnsafeMutablePointer(to: &painterZ) { pointer in
    
         }
          var has3: String! = String(cString: [99,97,112,97,98,105,108,105,116,105,101,115,0], encoding: .utf8)!
          var myloadingX: String! = String(cString: [108,101,97,115,101,0], encoding: .utf8)!
          _ = myloadingX
         secondlabelc = "\(1 << (Swift.min(1, has3.count)))"
         myloadingX.append("\(contO.count)")
         contO = "\(2 - secondlabelc.count)"
         interval_mI.append("\(contO.count)")
      repeat {
          var contextq: Int = 1
         withUnsafeMutablePointer(to: &contextq) { pointer in
    
         }
         contO.append("\((Int(isdeepseekG > 392780767.0 || isdeepseekG < -392780767.0 ? 30.0 : isdeepseekG) - interval_mI.count))")
         contextq ^= (contO == (String(cString:[51,0], encoding: .utf8)!) ? secondlabelG.values.count : contO.count)
         if contO == (String(cString:[107,108,102,105,103,113,110,0], encoding: .utf8)!) {
            break
         }
      } while (contO == (String(cString:[107,108,102,105,103,113,110,0], encoding: .utf8)!)) && (contO.hasPrefix("\(isdeepseekG)"))
          var num0: Int = 0
         secondlabelc = "\(contO.count)"
         num0 >>= Swift.min(2, labs((secondlabelG.count + Int(isdeepseekG > 168777228.0 || isdeepseekG < -168777228.0 ? 60.0 : isdeepseekG))))
         secondlabelc = "\(secondlabelc.count ^ 1)"
      repeat {
         secondlabelc = "\(2 | interval_mI.count)"
         if (String(cString:[53,56,101,110,51,117,51,121,109,50,0], encoding: .utf8)!) == secondlabelc {
            break
         }
      } while (2 >= (secondlabelc.count - secondlabelG.count)) && ((String(cString:[53,56,101,110,51,117,51,121,109,50,0], encoding: .utf8)!) == secondlabelc)
      while ((contO.count | 2) <= 1) {
          var msgc: String! = String(cString: [102,116,115,97,117,120,0], encoding: .utf8)!
         contO = "\(3 << (Swift.min(2, secondlabelG.count)))"
         msgc = "\(2)"
         break
      }
      reflect4 += (Double(titlelabelk == (String(cString:[109,0], encoding: .utf8)!) ? titlelabelk.count : secondlabelG.count))
      break
   }
        SVProgressHUD.dismiss()
      rowG.append("\((Int(reflect4 > 24614978.0 || reflect4 < -24614978.0 ? 16.0 : reflect4) - 2))")
        switch result {
        case.success(let model):
            
            if model.code == 200 {
                SVProgressHUD.showSuccess(withStatus: "删除成功")
      rowG = "\(3)"
                NotificationCenter.default.post(name: NSNotification.Name("aiRecordsDeleteSuccessNotificationName"), object: nil)
            }
            else {
                SVProgressHUD.showError(withStatus: model.msg);
            }
            
        case.failure(_):
            SVProgressHUD.showError(withStatus: "接口请求错误");
            break
        }
    }
}

struct IPhoneMain: Codable {
//var range_offset: Float = 0.0
//var constraintPadding: Double = 0.0
//var hasShow: Bool = false



    let msg: String?
    let code: Int?
    let total: Int?
    let rows: [EImage]?
}

struct EImage: Codable {
//var needs_offset: Double = 0.0
//var is_Brush: Bool = false
//var prefix_wq: Float = 0.0
//var listenPadding: Double = 0.0



    let id: String?
    let remark: String?
    let videoUrl: String?
    let videoDiamond: String?
    let videoType: Int?
    
}

struct QDWVoiceShou: Codable {
//var fixedSpace: Double = 0.0
//var picker_tag: Int = 0



    let msg: String?
    let code: Int?
    let total: Int?
    let rows: [PHMyloadingSetting]?
}

struct PHMyloadingSetting: Codable {
//var resources_c: Bool = false
//var show_min: Double = 0.0



    let id: String?
    let remark: String?
    let photoUrl: String?
    let templateId: String? 
    let photoType: Int? 
}





struct FHeaderStore: Codable {
//var enbale_Purchase: Bool = false
//var messagesIndex: Int = 0
//var rawing_max: Double = 0.0
//var materialSetting_str: String!



    let msg: String?
    let code: Int?
    let data: Bool?
}

func deleteMaterial(ids: NSArray) {
       var p_widthO: Int = 4
    var class_1n: Int = 3
   while ((p_widthO << (Swift.min(5, labs(class_1n)))) < 3 && 1 < (3 << (Swift.min(4, labs(class_1n))))) {
      p_widthO %= Swift.max(class_1n + p_widthO, 4)
      break
   }

      p_widthO /= Swift.max(2, class_1n)
    var task = [String: Any]()
   if 2 < (p_widthO >> (Swift.min(labs(2), 2))) {
      class_1n %= Swift.max(1, 2)
   }
    task["ids"] = ids
   while ((5 * class_1n) < 3 || 1 < (5 * class_1n)) {
      p_widthO %= Swift.max(1, class_1n & 1)
      break
   }
    
    SVProgressHUD.show()
    LDelegate.shared.post(urlSuffix: "/img/deleteAiImgBj", body: task) { (result: Result<FHeaderStore, NetworkError>) in
        SVProgressHUD.dismiss()
        switch result {
        case.success(let model):
            
            if model.code == 200 {
                SVProgressHUD.showSuccess(withStatus: "删除成功")
                NotificationCenter.default.post(name: NSNotification.Name("MaterialDeleteSuccessNotificationName"), object: nil)
            }
            else {
                SVProgressHUD.showError(withStatus: model.msg);
            }
            
        case.failure(_):
            SVProgressHUD.showError(withStatus: "接口请求错误");
            break
        }
    }
}

func uploadMaterialImages(image: UIImage) {
       var awakeE: String! = String(cString: [102,97,105,108,101,100,0], encoding: .utf8)!
   withUnsafeMutablePointer(to: &awakeE) { pointer in
          _ = pointer.pointee
   }
    var settingG: String! = String(cString: [105,110,115,116,97,108,108,115,0], encoding: .utf8)!
   if awakeE != settingG {
      settingG = "\(2)"
   }

   repeat {
      settingG.append("\(settingG.count / 3)")
      if (String(cString:[105,103,53,107,121,52,48,116,0], encoding: .utf8)!) == settingG {
         break
      }
   } while ((String(cString:[105,103,53,107,121,52,48,116,0], encoding: .utf8)!) == settingG) && (settingG.count >= 3)
    SVProgressHUD.show()
    LDelegate.shared.uploadImages(images: [image]) { result in
   if awakeE.contains(settingG) {
       var orginS: Int = 3
       var namen: String! = String(cString: [113,117,97,108,105,102,121,0], encoding: .utf8)!
         namen = "\(namen.count << (Swift.min(4, labs(orginS))))"
          var bonk5: String! = String(cString: [99,111,108,115,101,116,0], encoding: .utf8)!
          var detailslabell: String! = String(cString: [115,111,117,114,99,101,105,100,0], encoding: .utf8)!
         orginS <<= Swift.min(labs(2), 1)
         bonk5 = "\(namen.count & 2)"
         detailslabell.append("\(detailslabell.count + bonk5.count)")
         orginS %= Swift.max(namen.count, 4)
      while (!namen.contains("\(orginS)")) {
         namen.append("\(orginS)")
         break
      }
      while (orginS >= 2) {
         orginS &= 3
         break
      }
      while (1 <= (namen.count + 5) && (orginS + 5) <= 2) {
         orginS &= 3
         break
      }
      settingG = "\(namen.count)"
   }
        SVProgressHUD.dismiss()
        switch result {
        case .success(let body):
            if let dit = body as? NSDictionary, let code = dit["code"] as? Int {
                if code == 200 {
                    
                    if let array = dit["data"] as? [NSDictionary] {
                        for obj in array {
                            let window_2 = obj["url"] as! String
                            uploadMaterial(imgUrl: window_2)
                        }
                    }
                }
                else {
                    if let msg: String = dit["msg"] as? String {
                        SVProgressHUD.showError(withStatus: msg)
                    }
                }
            }else { }
            
        case.failure(_):
            break
        }
    }
}

func uploadMaterial(imgUrl: String) {
       var tapZ: [Any]! = [270, 167]
    var charsb: String! = String(cString: [109,111,110,111,116,111,110,105,99,0], encoding: .utf8)!
   withUnsafeMutablePointer(to: &charsb) { pointer in
    
   }
    var photoW: String! = String(cString: [99,97,112,97,98,108,101,0], encoding: .utf8)!
      tapZ.append(((String(cString:[103,0], encoding: .utf8)!) == charsb ? charsb.count : photoW.count))

   for _ in 0 ..< 3 {
      charsb.append("\(charsb.count)")
   }
    var task = [String: Any]()
   for _ in 0 ..< 2 {
      tapZ = [tapZ.count | 3]
   }
    task["imgUrl"] = imgUrl
       var m_layer3: Bool = true
      for _ in 0 ..< 1 {
          var jiao0: Float = 5.0
         m_layer3 = 28.19 >= jiao0 || !m_layer3
      }
       var weixinlabelD: Float = 1.0
      withUnsafeMutablePointer(to: &weixinlabelD) { pointer in
    
      }
         weixinlabelD *= (Float(Int(weixinlabelD > 62161530.0 || weixinlabelD < -62161530.0 ? 86.0 : weixinlabelD) << (Swift.min(2, labs(1)))))
      tapZ.append(tapZ.count * charsb.count)
    
    SVProgressHUD.show()
    LDelegate.shared.normalPost(urlSuffix: "/img/addAiImgBj", body: task) { result in
      charsb = "\((charsb == (String(cString:[97,0], encoding: .utf8)!) ? charsb.count : tapZ.count))"
        SVProgressHUD.dismiss()
      photoW = "\(photoW.count % 2)"
        switch result {
        case.success(let model):
            
            if let obj = model as? NSDictionary, let code = obj["code"] as? Int {
                if code == 200 {
                    NotificationCenter.default.post(name: NSNotification.Name("MaterialUploadSuccessNotificationName"), object: nil)
                }
                else {
                    SVProgressHUD.showError(withStatus: obj["msg"] as? String)
                }
            }
            break
        case.failure(_):
            SVProgressHUD.showError(withStatus: "接口请求出错")
      photoW.append("\(photoW.count % (Swift.max(photoW.count, 10)))")
            break
        }
    }
}

func aiChangeFacebookVideo(videoUrl: String, videoId: String, videoType: String) {
       var calendarP: Int = 5
    _ = calendarP
    var rightB: [String: Any]! = [String(cString: [100,105,115,99,117,115,115,0], encoding: .utf8)!:[String(cString: [118,111,116,101,114,0], encoding: .utf8)!:74, String(cString: [111,102,102,101,116,0], encoding: .utf8)!:696, String(cString: [109,97,116,99,104,105,110,102,111,0], encoding: .utf8)!:521]]
    _ = rightB
   repeat {
      rightB = ["\(rightB.count)": calendarP]
      if 1788479 == rightB.count {
         break
      }
   } while (rightB.values.contains { $0 as? Int == calendarP }) && (1788479 == rightB.count)

       var cellsn: String! = String(cString: [119,101,101,107,100,97,121,115,0], encoding: .utf8)!
      withUnsafeMutablePointer(to: &cellsn) { pointer in
             _ = pointer.pointee
      }
      repeat {
          var actionV: Double = 2.0
          var applyK: String! = String(cString: [105,115,112,97,116,99,104,0], encoding: .utf8)!
          var barW: [String: Any]! = [String(cString: [100,101,97,100,108,105,110,101,0], encoding: .utf8)!:String(cString: [109,117,108,114,101,115,0], encoding: .utf8)!, String(cString: [99,111,110,116,105,110,117,111,117,115,0], encoding: .utf8)!:String(cString: [117,110,99,108,105,112,112,101,100,0], encoding: .utf8)!, String(cString: [117,98,115,99,114,105,98,101,114,0], encoding: .utf8)!:String(cString: [110,111,97,115,109,0], encoding: .utf8)!]
         cellsn.append("\(barW.values.count % (Swift.max(applyK.count, 3)))")
         actionV -= Double(2 * applyK.count)
         if cellsn.count == 200486 {
            break
         }
      } while (cellsn.count == 200486) && (cellsn.count < cellsn.count)
      for _ in 0 ..< 3 {
         cellsn.append("\(cellsn.count)")
      }
         cellsn.append("\(cellsn.count | 2)")
      rightB = ["\(rightB.values.count)": 1]

   while (rightB["\(calendarP)"] == nil) {
      calendarP += calendarP << (Swift.min(labs(1), 2))
      break
   }
    var task = [String: Any]()
    task["videoUrl"] = videoUrl
    task["templateId"] = videoId
    task["videoType"] = videoType
    
    SVProgressHUD.show()
    LDelegate.shared.post(urlSuffix: "/aiVideo/aiVideo", body: task) { (result: Result<OZResponseLean, NetworkError>) in
        SVProgressHUD.dismiss()
        switch result {
        case.success(let model):
            
            if model.code == 200 {
                getpaintingPicture(taskId: model.data!)
            }
            else if model.code == 500 {
                
                if model.msg == "ai.ios.drawing.sum" {
                    SVProgressHUD.dismiss()
                    if isGuidance == 1 {
                        SVProgressHUD.showError(withStatus: "积分不足，请购买积分")
                        NotificationCenter.default.post(name: NSNotification.Name("enterPointControllerNotificationName"), object: nil)
                    }
                    else {
                        SVProgressHUD.showError(withStatus: "今日次数已用完")
                    }
                }
                else if model.msg == "ai.ios.drawing.vip" {
                    NotificationCenter.default.post(name: NSNotification.Name("enterMemberControllerNotificationName"), object: nil)
                }
                else {
                    SVProgressHUD.showError(withStatus: model.msg)
                }
            }
            else {
                SVProgressHUD.showError(withStatus: model.msg);
            }
            
        case.failure(_):
            SVProgressHUD.showError(withStatus: "接口请求错误");
            break
        }
    }
}

func aiPortraitfacebook(photoUrl: String, photoId: String, photoType: String) {
       var servicex: Int = 1
    _ = servicex
    var portraitx: [Any]! = [String(cString: [105,116,101,114,97,116,111,114,0], encoding: .utf8)!, String(cString: [104,111,110,101,121,115,119,97,112,0], encoding: .utf8)!, String(cString: [112,117,108,115,101,97,117,100,105,111,115,121,109,98,111,108,116,97,98,108,101,0], encoding: .utf8)!]
    var changeh: Bool = true
   withUnsafeMutablePointer(to: &changeh) { pointer in
          _ = pointer.pointee
   }
   if 4 >= portraitx.count {
       var basec: String! = String(cString: [99,104,111,115,101,0], encoding: .utf8)!
       var userk: Double = 1.0
       _ = userk
       var restoreR: Double = 5.0
       var instanceo: Float = 5.0
       _ = instanceo
       var headT: Double = 2.0
          var verityI: Float = 5.0
         withUnsafeMutablePointer(to: &verityI) { pointer in
                _ = pointer.pointee
         }
          var voiceN: String! = String(cString: [98,108,97,107,101,98,0], encoding: .utf8)!
          var imgY: Int = 4
         restoreR -= (Double(imgY & Int(restoreR > 274830425.0 || restoreR < -274830425.0 ? 13.0 : restoreR)))
         verityI *= (Float(Int(instanceo > 374685752.0 || instanceo < -374685752.0 ? 3.0 : instanceo) % (Swift.max(10, voiceN.count))))
         voiceN = "\((2 % (Swift.max(5, Int(headT > 262643364.0 || headT < -262643364.0 ? 95.0 : headT)))))"
      repeat {
          var restores: Double = 3.0
         instanceo += (Float(Int(headT > 387754361.0 || headT < -387754361.0 ? 72.0 : headT) ^ Int(userk > 183988611.0 || userk < -183988611.0 ? 93.0 : userk)))
         restores -= Double(basec.count)
         if instanceo == 58597.0 {
            break
         }
      } while ((userk / 1.23) >= 4.12 || (1.23 - instanceo) >= 3.88) && (instanceo == 58597.0)
      if (headT / 2.59) >= 1.75 || 3 >= (Int(headT > 121349474.0 || headT < -121349474.0 ? 10.0 : headT) / 1) {
         basec.append("\((Int(headT > 200140526.0 || headT < -200140526.0 ? 88.0 : headT) + Int(instanceo > 242057821.0 || instanceo < -242057821.0 ? 47.0 : instanceo)))")
      }
      while (Double(instanceo) <= headT) {
         headT -= (Double(Int(userk > 152459710.0 || userk < -152459710.0 ? 58.0 : userk)))
         break
      }
         userk -= (Double(3 * Int(instanceo > 161738301.0 || instanceo < -161738301.0 ? 7.0 : instanceo)))
      repeat {
          var u_countP: Double = 2.0
         instanceo += Float(1)
         u_countP /= Swift.max((Double(Int(instanceo > 205764678.0 || instanceo < -205764678.0 ? 83.0 : instanceo) % (Swift.max(basec.count, 8)))), 2)
         if 1207110.0 == instanceo {
            break
         }
      } while (1207110.0 == instanceo) && ((Double(instanceo) * headT) > 2.37)
      while (5.16 <= instanceo) {
         instanceo /= Swift.max((Float(Int(userk > 239542801.0 || userk < -239542801.0 ? 62.0 : userk))), 1)
         break
      }
      if 2.80 >= (restoreR * 5.1) || 3.37 >= (5.1 / (Swift.max(1, restoreR))) {
          var statew: Double = 2.0
          var recordsi: Bool = true
          _ = recordsi
          var delete_slv: Float = 4.0
          var socketP: String! = String(cString: [115,121,110,99,97,98,108,101,0], encoding: .utf8)!
         withUnsafeMutablePointer(to: &socketP) { pointer in
                _ = pointer.pointee
         }
         restoreR += Double(socketP.count << (Swift.min(labs(3), 1)))
         statew += (Double(socketP.count << (Swift.min(5, labs(Int(restoreR > 213918580.0 || restoreR < -213918580.0 ? 1.0 : restoreR))))))
         recordsi = delete_slv >= 3.31
         delete_slv /= Swift.max((Float(3 << (Swift.min(4, labs(Int(instanceo > 204425727.0 || instanceo < -204425727.0 ? 37.0 : instanceo)))))), 3)
      }
          var a_imagee: Double = 4.0
         withUnsafeMutablePointer(to: &a_imagee) { pointer in
                _ = pointer.pointee
         }
         basec = "\((Int(headT > 276665193.0 || headT < -276665193.0 ? 54.0 : headT)))"
         a_imagee *= (Double(Int(headT > 142659514.0 || headT < -142659514.0 ? 63.0 : headT) & Int(userk > 87749219.0 || userk < -87749219.0 ? 44.0 : userk)))
      for _ in 0 ..< 3 {
         restoreR /= Swift.max(2, Double(2))
      }
      if 3.88 > (Double(basec.count) - restoreR) && 3 > (3 - Int(restoreR > 135896221.0 || restoreR < -135896221.0 ? 8.0 : restoreR)) {
         restoreR *= (Double(1 % (Swift.max(Int(headT > 172754106.0 || headT < -172754106.0 ? 32.0 : headT), 9))))
      }
         basec = "\(2)"
         instanceo /= Swift.max((Float(Int(userk > 359071636.0 || userk < -359071636.0 ? 53.0 : userk))), 2)
      repeat {
          var sublyout2: String! = String(cString: [99,111,110,115,101,99,117,116,105,118,101,0], encoding: .utf8)!
          var receiveQ: String! = String(cString: [115,104,105,109,109,101,114,0], encoding: .utf8)!
          var setingF: Int = 1
          var storeg: String! = String(cString: [99,97,109,112,97,105,103,110,0], encoding: .utf8)!
          var second4: Int = 2
         restoreR += Double(basec.count)
         sublyout2 = "\((Int(headT > 108389016.0 || headT < -108389016.0 ? 97.0 : headT) / 2))"
         receiveQ = "\(setingF)"
         setingF /= Swift.max(3, (Int(instanceo > 143429164.0 || instanceo < -143429164.0 ? 4.0 : instanceo)))
         storeg = "\(2)"
         second4 -= setingF * storeg.count
         if restoreR == 4698061.0 {
            break
         }
      } while (restoreR == 4698061.0) && ((basec.count - Int(restoreR > 78455458.0 || restoreR < -78455458.0 ? 60.0 : restoreR)) <= 2)
      repeat {
          var descript4: Int = 4
          var resumptionH: String! = String(cString: [98,105,116,101,120,97,99,116,0], encoding: .utf8)!
          _ = resumptionH
          var ollectionM: String! = String(cString: [112,114,111,100,115,0], encoding: .utf8)!
          _ = ollectionM
          var convertedc: Int = 0
          _ = convertedc
         userk *= (Double(Int(instanceo > 161024314.0 || instanceo < -161024314.0 ? 86.0 : instanceo)))
         descript4 <<= Swift.min(5, labs((1 | Int(instanceo > 191492101.0 || instanceo < -191492101.0 ? 59.0 : instanceo))))
         resumptionH = "\((resumptionH.count << (Swift.min(5, labs(Int(headT > 49252216.0 || headT < -49252216.0 ? 29.0 : headT))))))"
         ollectionM = "\(2 | convertedc)"
         convertedc |= convertedc
         if 490981.0 == userk {
            break
         }
      } while (userk > headT) && (490981.0 == userk)
      portraitx.append(3)
   }

      servicex /= Swift.max(2, ((changeh ? 1 : 3) & 3))

      portraitx.append(((changeh ? 1 : 4) / (Swift.max(9, portraitx.count))))
    var task = [String: Any]()
   while (servicex <= portraitx.count) {
       var animau: String! = String(cString: [112,105,112,101,108,111,115,115,0], encoding: .utf8)!
       var querysN: String! = String(cString: [105,110,99,108,117,100,101,100,0], encoding: .utf8)!
      withUnsafeMutablePointer(to: &querysN) { pointer in
    
      }
       var handleZ: Float = 3.0
       var add_: String! = String(cString: [98,97,115,101,0], encoding: .utf8)!
       _ = add_
       var setingi: Double = 5.0
         setingi *= (Double(Int(setingi > 384872384.0 || setingi < -384872384.0 ? 3.0 : setingi) / (Swift.max(7, add_.count))))
      repeat {
         add_ = "\(add_.count)"
         if add_.count == 1479171 {
            break
         }
      } while (add_.count == 1479171) && ((Double(add_.count) - setingi) >= 3.36)
      for _ in 0 ..< 2 {
         add_ = "\(((String(cString:[77,0], encoding: .utf8)!) == querysN ? querysN.count : Int(handleZ > 54970185.0 || handleZ < -54970185.0 ? 100.0 : handleZ)))"
      }
         handleZ /= Swift.max((Float((String(cString:[74,0], encoding: .utf8)!) == add_ ? Int(setingi > 223360962.0 || setingi < -223360962.0 ? 32.0 : setingi) : add_.count)), 4)
          var rotationW: Int = 0
         animau = "\(querysN.count)"
         rotationW *= rotationW
      if 1 == add_.count {
          var waterw: String! = String(cString: [111,112,101,114,97,116,105,111,110,0], encoding: .utf8)!
          var pickede: String! = String(cString: [100,101,99,107,108,105,110,107,0], encoding: .utf8)!
          var recordO: Double = 1.0
         withUnsafeMutablePointer(to: &recordO) { pointer in
    
         }
          var mineM: Double = 0.0
          var outuB: Double = 2.0
         handleZ /= Swift.max(1, (Float(querysN == (String(cString:[69,0], encoding: .utf8)!) ? Int(handleZ > 294676606.0 || handleZ < -294676606.0 ? 4.0 : handleZ) : querysN.count)))
         waterw = "\(pickede.count | waterw.count)"
         pickede.append("\((Int(outuB > 95284755.0 || outuB < -95284755.0 ? 28.0 : outuB)))")
         recordO *= Double(animau.count * 2)
         mineM -= Double(1)
         outuB /= Swift.max(Double(animau.count), 1)
      }
         animau.append("\((add_.count % (Swift.max(7, Int(setingi > 128408236.0 || setingi < -128408236.0 ? 97.0 : setingi)))))")
      while (add_.count > querysN.count) {
         add_.append("\((Int(handleZ > 254764197.0 || handleZ < -254764197.0 ? 23.0 : handleZ)))")
         break
      }
         handleZ /= Swift.max((Float(3 << (Swift.min(4, labs(Int(setingi > 257413809.0 || setingi < -257413809.0 ? 96.0 : setingi)))))), 5)
       var promptl: [String: Any]! = [String(cString: [99,111,110,116,114,105,98,117,116,105,110,103,0], encoding: .utf8)!:775, String(cString: [109,97,99,114,111,98,108,111,99,107,0], encoding: .utf8)!:885, String(cString: [98,97,100,114,101,113,0], encoding: .utf8)!:47]
      withUnsafeMutablePointer(to: &promptl) { pointer in
             _ = pointer.pointee
      }
       var imageviewb: [String: Any]! = [String(cString: [109,112,101,103,118,108,99,0], encoding: .utf8)!:372, String(cString: [117,112,103,114,97,100,101,100,0], encoding: .utf8)!:870]
       _ = imageviewb
      repeat {
         add_ = "\(promptl.count - 3)"
         if add_ == (String(cString:[55,110,100,0], encoding: .utf8)!) {
            break
         }
      } while (2 <= add_.count) && (add_ == (String(cString:[55,110,100,0], encoding: .utf8)!))
      if animau.contains(add_) {
         animau = "\(promptl.count % (Swift.max(2, 10)))"
      }
          var pagei: Bool = true
         withUnsafeMutablePointer(to: &pagei) { pointer in
    
         }
          var numberX: Double = 4.0
         imageviewb = ["\(setingi)": (Int(setingi > 282099881.0 || setingi < -282099881.0 ? 42.0 : setingi) % 3)]
         pagei = (String(cString:[51,0], encoding: .utf8)!) == add_
         numberX -= (Double((String(cString:[120,0], encoding: .utf8)!) == querysN ? Int(setingi > 129518292.0 || setingi < -129518292.0 ? 65.0 : setingi) : querysN.count))
      repeat {
         add_.append("\(imageviewb.keys.count << (Swift.min(labs(1), 3)))")
         if add_.count == 3375824 {
            break
         }
      } while (add_.count == 3375824) && (3 <= (add_.count << (Swift.min(labs(1), 2))))
         add_.append("\(add_.count)")
      portraitx.append((add_ == (String(cString:[121,0], encoding: .utf8)!) ? add_.count : querysN.count))
      break
   }
    task["photoUrl"] = photoUrl
   if 3 <= (servicex >> (Swift.min(labs(1), 3))) {
       var completionw: Bool = false
      for _ in 0 ..< 3 {
         completionw = !completionw
      }
         completionw = !completionw
         completionw = !completionw
      changeh = portraitx.count < servicex
   }
    task["templateId"] = photoId
    task["photoType"] = photoType
    
    SVProgressHUD.show()
    LDelegate.shared.post(urlSuffix: "/aiPhoto/aiVideo", body: task) { (result: Result<OZResponseLean, NetworkError>) in
        SVProgressHUD.dismiss()
        switch result {
        case.success(let model):
            
            if model.code == 200 {
                getpaintingPicture(taskId: model.data!)
            }
            else if model.code == 500 {
                if model.msg == "ai.ios.drawing.sum" {
                    SVProgressHUD.dismiss()
                    if isGuidance == 1 {
                        SVProgressHUD.showError(withStatus: "积分不足，请购买积分")
                        NotificationCenter.default.post(name: NSNotification.Name("enterPointControllerNotificationName"), object: nil)
                    }
                    else {
                        SVProgressHUD.showError(withStatus: "今日次数已用完")
                    }
                }
                else if model.msg == "ai.ios.drawing.vip" {
                    NotificationCenter.default.post(name: NSNotification.Name("enterMemberControllerNotificationName"), object: nil)
                }
                else {
                    SVProgressHUD.showError(withStatus: model.msg)
                }
            }
            else {
                SVProgressHUD.showError(withStatus: model.msg);
            }
            
        case.failure(_):
            SVProgressHUD.showError(withStatus: "接口请求错误");
            break
        }
    }
}


func saveVideoToPhotos(videoURL: URL) {
       var chuangc: Double = 3.0
    var safeL: Float = 5.0
       var compressedz: String! = String(cString: [100,121,110,97,109,105,99,97,108,108,121,0], encoding: .utf8)!
       var kout1: [String: Any]! = [String(cString: [115,101,108,101,99,116,105,111,110,0], encoding: .utf8)!:343]
       _ = kout1
         kout1 = ["\(kout1.values.count)": compressedz.count & kout1.values.count]
          var orginb: Float = 0.0
          var tableheaderr: String! = String(cString: [102,104,116,120,0], encoding: .utf8)!
          var addI: String! = String(cString: [98,102,115,116,109,0], encoding: .utf8)!
         compressedz.append("\(compressedz.count / (Swift.max(1, 4)))")
         orginb -= Float(compressedz.count % (Swift.max(addI.count, 6)))
         tableheaderr.append("\(tableheaderr.count & addI.count)")
         compressedz.append("\(1)")
      for _ in 0 ..< 3 {
          var sectionq: Double = 5.0
         withUnsafeMutablePointer(to: &sectionq) { pointer in
                _ = pointer.pointee
         }
          var zhidingesf: String! = String(cString: [115,117,98,105,116,101,109,115,0], encoding: .utf8)!
          var fileQ: String! = String(cString: [97,114,99,0], encoding: .utf8)!
         kout1["\(sectionq)"] = (3 | Int(sectionq > 114773131.0 || sectionq < -114773131.0 ? 91.0 : sectionq))
         zhidingesf.append("\(zhidingesf.count)")
         fileQ.append("\(compressedz.count * 3)")
      }
         kout1 = ["\(kout1.values.count)": compressedz.count]
       var didw: Float = 1.0
         didw *= Float(1)
      safeL -= Float(3)

      chuangc *= (Double(1 & Int(safeL > 351893700.0 || safeL < -351893700.0 ? 40.0 : safeL)))
    PHPhotoLibrary.shared().performChanges({
        PHAssetChangeRequest.creationRequestForAssetFromVideo(atFileURL: videoURL)
    }) { saved, error in
        SVProgressHUD.dismiss()
        if saved {
            SVProgressHUD.showSuccess(withStatus: "保存成功")
        } else {
            SVProgressHUD.showError(withStatus: "保存失败")
            print("Error saving video: \(error?.localizedDescription ?? "")")
        }
    }
    
}

func saveImageToPhotos(image: UIImage) {
       var collectr: Bool = false
    var workA: Float = 0.0
    _ = workA
       var btnt: String! = String(cString: [105,103,104,108,105,103,104,116,115,0], encoding: .utf8)!
       var attributesR: [String: Any]! = [String(cString: [100,112,116,114,115,0], encoding: .utf8)!:764, String(cString: [100,101,116,97,105,108,101,100,0], encoding: .utf8)!:621]
       var pickerm: String! = String(cString: [105,110,115,116,114,0], encoding: .utf8)!
      withUnsafeMutablePointer(to: &pickerm) { pointer in
    
      }
          var bigI: Float = 0.0
          var gnews3: Bool = true
          _ = gnews3
         attributesR["\(bigI)"] = (Int(bigI > 223739719.0 || bigI < -223739719.0 ? 75.0 : bigI) >> (Swift.min(pickerm.count, 2)))
         gnews3 = attributesR.values.count > 19 || btnt.count > 19
         pickerm = "\(btnt.count)"
      while (2 >= (pickerm.count >> (Swift.min(1, attributesR.count))) && 5 >= (pickerm.count >> (Swift.min(labs(2), 3)))) {
         pickerm.append("\(attributesR.keys.count << (Swift.min(labs(2), 2)))")
         break
      }
          var leftbuttonN: String! = String(cString: [115,101,114,105,97,108,105,122,97,98,108,101,0], encoding: .utf8)!
          _ = leftbuttonN
         pickerm.append("\(pickerm.count)")
         leftbuttonN.append("\(attributesR.count)")
          var imgurlK: String! = String(cString: [99,111,112,121,98,97,99,107,0], encoding: .utf8)!
          _ = imgurlK
         btnt.append("\(btnt.count + pickerm.count)")
         imgurlK.append("\(2 - pickerm.count)")
         attributesR = ["\(attributesR.values.count)": 2]
       var ratioE: String! = String(cString: [104,105,103,104,108,105,103,104,116,97,98,108,101,0], encoding: .utf8)!
      withUnsafeMutablePointer(to: &ratioE) { pointer in
             _ = pointer.pointee
      }
       var mineI: String! = String(cString: [110,97,118,105,103,97,116,101,0], encoding: .utf8)!
      repeat {
         btnt.append("\(1)")
         if btnt.count == 795726 {
            break
         }
      } while (mineI == String(cString:[121,0], encoding: .utf8)! || 2 < btnt.count) && (btnt.count == 795726)
         btnt.append("\(2 + pickerm.count)")
         ratioE.append("\(3)")
      workA += (Float(1 % (Swift.max(3, (collectr ? 4 : 2)))))

      collectr = 53.41 < workA && collectr
        PHPhotoLibrary.shared().performChanges({
            let month = PHAssetChangeRequest.creationRequestForAsset(from: image)
   repeat {
      collectr = !collectr
      if collectr ? !collectr : collectr {
         break
      }
   } while (collectr ? !collectr : collectr) && (1.64 >= (workA / 5.76))
            month.creationDate = Date()
        }) { saved, error in
      collectr = !collectr
            if saved {
                SVProgressHUD.showSuccess(withStatus: "保存成功")
            } else {
                SVProgressHUD.showError(withStatus: "保存失败")
                print("Error saving image: \(error?.localizedDescription ?? "")")
            }
        }
    }

struct VCanvas: Codable {
//var allGundongDid_string: String?
//var bufferMargin: Double = 0.0
//var speak_space: Double = 0.0



    let msg: String?
    let code: Int?
    let total: Int?
    let rows: [ZCLRing]?
}

struct ZCLRing: Codable {
//var secondTrue_c_str: String!
//var alertPadding: Float = 0.0
//var replaceIndex: Int = 0




    let id: String?
    let userId: String?
    let damondSum: Int?
    let damondType: Int? 
    let damondState: Int? 
    let createTime: String?
}
